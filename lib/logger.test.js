require('qtests/setup');const qtests=require('qtests');const fs=require('fs');const winston=require('winston');const DailyRotateFile=require('winston-daily-rotate-file');function reload(){delete require.cache[require.resolve('./logger')];return require('./logger')}describe('Logger Utility',()=>{test('verifies should configure DailyRotateFile transport',()=>{let captured;const restore=qtests.stubMethod(winston,'createLogger',cfg=>{captured=cfg;return{transports:cfg.transports,debug(){},info(){},error(){}}});reload();restore();const hasRotate=captured.transports.some(t=>t instanceof DailyRotateFile);expect(hasRotate).toBe(true)})});