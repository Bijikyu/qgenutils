'use strict';const validatePassword=require('./validatePassword');describe('validatePassword',()=>{it('should return valid for strong passwords',()=>{const result=validatePassword('SecureP@ss1');expect(result.isValid).toBe(true);expect(result.errors).toEqual([]);expect(result.strength).toBe('strong')});it('should reject too short passwords',()=>{const result=validatePassword('Aa1!');expect(result.isValid).toBe(false);expect(result.errors).toContain('too_short')});it('should reject too long passwords',()=>{const result=validatePassword('A'.repeat(129)+'a1!');expect(result.isValid).toBe(false);expect(result.errors).toContain('too_long')});it('should require uppercase',()=>{const result=validatePassword('lowercase1!');expect(result.isValid).toBe(false);expect(result.errors).toContain('no_uppercase')});it('should require lowercase',()=>{const result=validatePassword('UPPERCASE1!');expect(result.isValid).toBe(false);expect(result.errors).toContain('no_lowercase')});it('should require numbers',()=>{const result=validatePassword('NoNumbers!');expect(result.isValid).toBe(false);expect(result.errors).toContain('no_number')});it('should require special characters',()=>{const result=validatePassword('NoSpecial1');expect(result.isValid).toBe(false);expect(result.errors).toContain('no_special')});it('should classify strength based on error count',()=>{const weak=validatePassword('Password1');expect(weak.strength).toBe('weak');const veryWeak=validatePassword('abc');expect(veryWeak.strength).toBe('very_weak')});it('should handle invalid input',()=>{expect(validatePassword(null).isValid).toBe(false);expect(validatePassword(null).errors).toContain('invalid_input');expect(validatePassword(null).strength).toBe('invalid');expect(validatePassword(undefined).isValid).toBe(false);expect(validatePassword(123).isValid).toBe(false)})})