const makeIdempotencyKey=require(./makeIdempotencyKey);describe('makeIdempotencyKey',()=>{it('should create key from single string part',()=>{expect(makeIdempotencyKey('user123')).toBe('user123')});it('should create key from multiple string parts',()=>{expect(makeIdempotencyKey('user','action','timestamp')).toBe('user:action:timestamp')});it('should create key from number parts',()=>{expect(makeIdempotencyKey(123,456)).toBe('123:456')});it('should create key from mixed string and number parts',()=>{expect(makeIdempotencyKey('order',12345,'create')).toBe('order:12345:create')});it('should trim whitespace from string parts',()=>{expect(makeIdempotencyKey(' user ',' action ')).toBe('user:action')});it('should handle zero as valid number',()=>{expect(makeIdempotencyKey('count',0)).toBe('count:0')});it('should handle negative numbers',()=>{expect(makeIdempotencyKey('offset',-100)).toBe('offset:-100')});it('should throw for no parts',()=>{expect(()=>makeIdempotencyKey()).toThrow('At least one part is required')});it('should throw for empty array',()=>{expect(()=>makeIdempotencyKey(...[])).toThrow('At least one part is required')});it('should throw for null part',()=>{expect(()=>makeIdempotencyKey('valid',null)).toThrow('Part 1 cannot be null or undefined')});it('should throw for undefined part',()=>{expect(()=>makeIdempotencyKey('valid',undefined)).toThrow('Part 1 cannot be null or undefined')});it('should throw for empty string part',()=>{expect(()=>makeIdempotencyKey('valid','')).toThrow('Part 1 cannot be empty or whitespace only')});it('should throw for whitespace-only string part',()=>{expect(()=>makeIdempotencyKey('valid',' ')).toThrow('Part 1 cannot be empty or whitespace only')});it('should throw for NaN',()=>{expect(()=>makeIdempotencyKey('valid',NaN)).toThrow('Part 1 must be a finite number')});it('should throw for Infinity',()=>{expect(()=>makeIdempotencyKey('valid',Infinity)).toThrow('Part 1 must be a finite number')});it('should throw for object part',()=>{expect(()=>makeIdempotencyKey('valid',{})).toThrow('Part 1 must be a string or number')});it('should throw for array part',()=>{expect(()=>makeIdempotencyKey('valid',[])).toThrow('Part 1 must be a string or number')});it('should throw for boolean part',()=>{expect(()=>makeIdempotencyKey('valid',true)).toThrow('Part 1 must be a string or number')});it('should produce deterministic output',()=>{const key1=makeIdempotencyKey('user',123,'action'),key2=makeIdempotencyKey('user',123,'action');expect(key1).toBe(key2)})});