'use strict';const measureEventLoopLag=require(./measureEventLoopLag);describe('measureEventLoopLag',()=>{it('should throw for non-function callback',()=>{expect(()=>measureEventLoopLag(null)).toThrow('Callback must be a function');expect(()=>measureEventLoopLag('string')).toThrow('Callback must be a function');expect(()=>measureEventLoopLag(123)).toThrow('Callback must be a function')});it('should call callback with a number',(done)=>{measureEventLoopLag((lag)=>{expect(typeof lag).toBe('number');done()})});it('should return non-negative lag',(done)=>{measureEventLoopLag((lag)=>{expect(lag).toBeGreaterThanOrEqual(0);done()})});it('should measure small lag under normal conditions',(done)=>{measureEventLoopLag((lag)=>{expect(lag).toBeLessThan(100);done()})})});