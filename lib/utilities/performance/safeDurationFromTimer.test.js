const safeDurationFromTimer=require(./safeDurationFromTimer);describe('safeDurationFromTimer',()=>{it('should return elapsed time from valid timer',()=>{const timer ={elapsed:()=>1234};expect(safeDurationFromTimer(timer)).toBe(1234)});it('should return 0 for undefined timer',()=>{expect(safeDurationFromTimer(undefined)).toBe(0)});it('should return 0 for null timer',()=>{expect(safeDurationFromTimer(null)).toBe(0)});it('should return 0 for timer without elapsed method',()=>{expect(safeDurationFromTimer({})).toBe(0);expect(safeDurationFromTimer({elapsed: 'not a function'})).toBe(0)});it('should return 0 when elapsed throws',()=>{const timer ={elapsed:()=>{throw new Error('fail')}};expect(safeDurationFromTimer(timer)).toBe(0)});it('should return 0 for non-object types',()=>{expect(safeDurationFromTimer('string')).toBe(0);expect(safeDurationFromTimer(123)).toBe(0);expect(safeDurationFromTimer([])).toBe(0)});it('should handle timer returning 0',()=>{const timer ={elapsed:()=>0};expect(safeDurationFromTimer(timer)).toBe(0)});it('should handle timer returning negative value',()=>{const timer ={elapsed:()=>-100};expect(safeDurationFromTimer(timer)).toBe(-100)})});