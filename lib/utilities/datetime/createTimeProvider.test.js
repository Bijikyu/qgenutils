const createTimeProvider=require(./createTimeProvider);describe('createTimeProvider',()=>{it('should return object with now,timestamp,and isoString methods',()=>{const provider=createTimeProvider();expect(typeof provider.now).toBe('function');expect(typeof provider.timestamp).toBe('function');expect(typeof provider.isoString).toBe('function')});it('should use real time by default',()=>{const provider=createTimeProvider(),before=Date.now(),ts=provider.timestamp(),after=Date.now();expect(ts).toBeGreaterThanOrEqual(before);expect(ts).toBeLessThanOrEqual(after)});it('should use injected time function',()=>{const fixedDate=new Date('2024-01-15T10:30:00Z'),provider=createTimeProvider(()=>fixedDate);expect(provider.now()).toEqual(fixedDate);expect(provider.timestamp()).toBe(fixedDate.getTime());expect(provider.isoString()).toBe('2024-01-15T10:30:00.000Z')});it('should call injected function each time',()=>{let callCount=0;const provider=createTimeProvider(()=>{callCount++;return new Date('2024-01-15T10:30:00Z')});provider.now();provider.now();provider.now();expect(callCount).toBe(3)});it('should handle non-function input by using default',()=>{const provider=createTimeProvider('not a function');expect(provider.now()instanceof Date).toBe(true)});it('should handle null input by using default',()=>{const provider=createTimeProvider(null);expect(provider.now()instanceof Date).toBe(true)})});